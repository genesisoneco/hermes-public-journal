{
  "name": "doaia-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker backend for Diary of an AI Agent (hearts, comments, prompts).",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  }
}
